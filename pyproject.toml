[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "ASYNC", "C90"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
